# Generate a Fernet key:
#   python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
SECRET_KEY=REPLACE_ME_WITH_FERNET_KEY

# Storage paths (inside container — matched to docker-compose volumes)
DB_PATH=/data/ribbon.db
UPLOAD_DIR=/uploads

# Server
APP_HOST=0.0.0.0
APP_PORT=8000
DEBUG=false
